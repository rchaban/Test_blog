%h1 Update your profile
.row
  .span6.offset3
    = form_for(@user) do |f|
      = render 'fields', f: f
      .field_label= f.label "Chose new avatar"
      #avatar= f.file_field :avatar
      = f.submit "Save changes", class: "btn btn-large btn-primary"