!!!
%html
  %head
    %title My Blog
  %body
    - if signed_in?
      .row
        %aside.span4
          %section
            = render 'shared/user_info'
          %section
            = render 'shared/micropost_form'
        .span8
          %h3 Micropost Feed
          = render 'shared/feed'
    - else
      .center
        %h1 Welcome
        %h2
          This is the home page for the My blog.
        = link_to "Sign up now!", signup_path, class: "btn btn-large btn-primary"